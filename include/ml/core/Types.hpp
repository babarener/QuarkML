#pragma once
#include <vector>

namespace ml {

// Minimal placeholders for now; can be swapped to Eigen later.
using Vector = std::vector<double>;
using Matrix = std::vector<std::vector<double>>;

} // namespace ml
